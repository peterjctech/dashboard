export interface CategoryProps {
    category: string;
    color: string;
    for: string;
}

export interface CategoryModel {
    category_id: string;
    category: string;
    color: string;
    for: string;
}

export interface Category {
    category_id: string;
    category: string;
    color: string;
    for: string;
}
