export interface ShortcutProps {
    shortcut: string;
    title: string;
    type: string;
    icon?: string;
}

export interface ShortcutModel {
    shortcut_id: string;
    shortcut: string;
    title: string;
    type: string;
}

export interface Shortcut {
    shortcut_id: string;
    shortcut: string;
    title: string;
    type: string;
    icon?: string;
}
