export interface ShoppingProps {
    item: string;
}

export interface ShoppingModel {
    item_id: string;
    item: string;
    color: string;
}

export interface Shopping {
    item_id: string;
    item: string;
    color: string;
}
