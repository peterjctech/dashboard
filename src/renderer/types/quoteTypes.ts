export interface QuoteProps {
    quote: string;
    author: string;
}

export interface QuoteModel {
    quote_id: string;
    quote: string;
}

export interface Quote {
    quote_id: string;
    quote: string;
}
