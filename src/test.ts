import {
    ActivityProps,
    EventCategoryProps,
    GoalProps,
    NoteProps,
    EventProps,
    HabitProps,
    QuoteProps,
    TicketProps,
    WorkoutProps,
    ReminderProps,
    ShortcutProps,
    AchievementProps,
    TicketCategoryProps,
} from "./interfaces";

// console.log(ActivityProps);
